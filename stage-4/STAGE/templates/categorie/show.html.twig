{% extends 'backtemplate.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1><font color="mediumturquoise">Tous les Catégories</font></h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
            <tr>
                <th>Descrip</th>
                <td>{{ categorie.descrip }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}">back to list</a>

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}">edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
